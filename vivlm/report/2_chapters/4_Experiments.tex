\chapter{THỰC NGHIỆM VÀ KẾT QUẢ}
\label{chap:experiments}

\section{Thiết lập và thước đo}
\label{sec:exp-setup}

Chất lượng ngôn ngữ nội tại đo bằng loss/PPL trên tập kiểm định 30,6M token
(cửa sổ tuần
tự, không lấy mẫu ngẫu nhiên) và BPC~\eqref{eq:bpc} khi so với baseline khác
tokenizer. Chất lượng đa phương thức đo bằng BLEU-4 và CIDEr
(\texttt{pycocoevalcap}) trên các tập kiểm tra UIT-ViIC, KTVIC (mô tả ảnh, 5
câu tham chiếu mỗi ảnh) và OpenViVQA (hỏi--đáp; dùng phân tập dev vì phân tập
test của cuộc thi VLSP không công bố đáp án); văn bản chuẩn hóa chữ thường và
khoảng trắng trước khi chấm --- nhất quán cho mọi hệ được so sánh. Giải mã so
sánh giữa tham lam và beam search $k{=}3$ (chuẩn hóa độ dài mũ 0,7).

\section{Kết quả tiền huấn luyện}
\label{sec:exp-pretrain}

\subsection{Đường cong học}

Hình~\ref{fig:pretrain-loss} trình bày loss huấn luyện và kiểm định theo số
bước. Ba quan sát định lượng: (i)~loss khởi điểm 10,09 khớp giá trị lý thuyết
của phân phối đều trên từ vựng ($\ln 20.480 = 9{,}93$ cộng nhiễu khởi tạo) ---
một phép ``sanity check'' cho cài đặt; (ii)~loss rơi rất nhanh trong nghìn
bước đầu (val 4,37 tại bước 250; 3,63 tại bước 500) rồi chuyển sang pha giảm
chậm dạng lũy thừa đúng như quy luật scaling (mục~\ref{sec:bg-scaling}), về
2,62 ở bước cuối; (iii)~val bám sát train (thậm chí thấp hơn nhẹ) suốt quá
trình --- mô hình 101M trên 3 tỷ token nằm sâu trong chế độ thiếu dữ liệu hơn
là quá khớp, đúng kỳ vọng khi mỗi mẫu chỉ được thấy một lần.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.85\textwidth]{figs/results/pretrain_loss.png}
	\caption{Loss huấn luyện (đường liền) và kiểm định (điểm) theo số bước,
	6.000 bước $\approx$ 2,95 tỷ token. Val giảm đơn điệu từ 4,37 (bước 250)
	về 2,62 (bước 6.000). Nguồn: \texttt{pretrain\_log.csv}.}
	\label{fig:pretrain-loss}
\end{figure}

\begin{table}[H]
	\centering
	\caption{Chất lượng ngôn ngữ nội tại của ViGPT-nano sau tiền huấn luyện,
	so với baseline GPT-2 tiếng Việt huấn luyện sẵn. PPL không so được giữa hai
	tokenizer khác nhau nên cột so sánh dùng BPC (mục~\ref{sec:bg-lm}), đo đối
	xứng trên cùng 464 nghìn ký tự văn bản kiểm định sạch, mỗi mô hình dùng
	tokenizer của chính nó.}
	\label{tab:ppl-bpc}
	\begin{tabular}{lccc}
		\hline
		\textbf{Mô hình} & \textbf{Tham số} & \textbf{PPL (val)} & \textbf{BPC} \\
		\hline
		ViGPT-nano (đề tài) & 101M & 13,91 & \textbf{0,987} \\
		NlpHUST/gpt2-vietnamese \autocite{nlphust2021gpt2vi} & 124M & $\approx$13,8$^\dagger$ & 3,329 \\
		\hline
		\multicolumn{4}{l}{\footnotesize $\dagger$ PPL của baseline tính trên
		tokenizer riêng, chỉ để đối chiếu độ khó mỗi token --- xem diễn giải.} \\
	\end{tabular}
\end{table}

Con số đáng bàn: BPC của ViGPT-nano ($0{,}987$) thấp hơn baseline ($3{,}329$)
tới $3{,}4$ lần --- một biên độ lớn bất thường cần được giải thích thay vì
nhận là ``thắng''. Nguyên nhân gần như hoàn toàn nằm ở \emph{bộ tách từ}:
kiểm tra trực tiếp cho thấy NlpHUST/gpt2-vietnamese dùng nguyên tokenizer
byte-level của GPT-2 tiếng Anh (từ vựng 50.257, fertility 3,97 token/từ ở
bảng~\ref{tab:fertility}), trong khi ViGPT-nano dùng BPE tiếng Việt tự huấn
luyện (fertility 1,16). Quy đổi ngược, \emph{độ khó mỗi token} (perplexity)
của hai mô hình xấp xỉ nhau ($\approx$13,8 so với 13,91); toàn bộ chênh lệch
BPC đến từ việc tokenizer tiếng Anh phải chi $\approx$3,4 lần nhiều token hơn
cho mỗi ký tự tiếng Việt. Đây chính là minh chứng định lượng cho giá trị của
một tokenizer bản ngữ: cùng năng lực dự đoán mỗi token, mô hình có tokenizer
tiếng Việt nén văn bản hiệu quả hơn hẳn tính trên ký tự.

\subsection{Chất lượng định tính}

Bảng~\ref{tab:samples} liệt kê văn bản mô hình sinh (top-$p$ 0,9) từ vài câu
mồi tiếng Việt. Kỳ vọng đặt đúng quy mô: mô hình 101M huấn luyện 3 tỷ token
viết được câu đúng ngữ pháp, mạch lạc cục bộ, nhưng kiến thức thế giới còn
nông --- ``đúng dáng văn, ngây ngô nội dung'' là mức chuẩn của lớp mô hình
nano (so sánh: nanoVLM \autocite{wiedmann2025nanovlm} chọn backbone SmolLM2
135M huấn luyện tới 2.000 tỷ token).

\begin{table}[H]
	\centering
	\caption{Mẫu văn bản sinh từ ViGPT-nano (top-$p = 0{,}9$).}
	\label{tab:samples}
	\begin{tabularx}{\textwidth}{lX}
		\hline
		\textbf{Câu mồi} & \textbf{Văn bản sinh (trích)} \\
		\hline
		``Hà Nội là'' & \emph{\ldots vùng đất có nhiều di tích lịch sử -- văn hóa. Với các di tích lịch sử -- văn hóa được xếp hạng, chúng ta đã và đang gìn giữ, bảo vệ và phát huy giá trị của di tích một cách trọn vẹn.} \\
		``Trí tuệ nhân tạo'' & \emph{\ldots của Google để giữ cho công việc kinh doanh của bạn không bị ảnh hưởng bởi các yêu cầu khách hàng mới. Những yếu tố quan trọng nhất ảnh hưởng đến kết quả tìm kiếm của Google bao gồm\ldots} \\
		``Món phở bò'' & \emph{\ldots nướng cà ri cơm này, vừa ngon, vừa bổ, lại thơm ngon nữa đấy. Cơm cuộn chay là món ăn đặc biệt được nhiều người yêu thích, được bày bán khắp các chợ mạng, quán ăn Việt Nam.} \\
		\hline
	\end{tabularx}
\end{table}

\section{Hiệu quả bộ tách từ}
\label{sec:exp-tokenizer}

Bảng~\ref{tab:fertility} so sánh fertility trên cùng một mẫu văn bản tiếng
Việt giữ lại từ corpus. Kết quả xác nhận cả hai giả thuyết đặt ra:
(i)~tokenizer tự huấn luyện đạt fertility 1,160 --- ngang PhoGPT (1,166, cùng
cỡ từ vựng, cùng byte-level, khác dữ liệu huấn luyện), cho thấy 0,35\,GB văn
bản là đủ để BPE học thống kê cặp byte tiếng Việt; (ii)~tokenizer GPT-2 tiếng
Anh chịu fertility 3,970 --- cao gấp $3{,}4$ lần do phải phân rã ký tự có dấu
về nhiều byte. Con số 3,4 lần này chính là hệ số giải thích khoảng cách BPC ở
bảng~\ref{tab:ppl-bpc}.

\begin{table}[H]
	\centering
	\caption{Fertility (token/từ, thấp hơn là tốt hơn) trên cùng văn bản tiếng Việt.}
	\label{tab:fertility}
	\begin{tabular}{lcc}
		\hline
		\textbf{Tokenizer} & \textbf{Từ vựng} & \textbf{Fertility} \\
		\hline
		BPE tự huấn luyện (đề tài) & 20.480 & \textbf{1,160} \\
		PhoGPT \autocite{nguyen2023phogpt} & 20.480 & 1,166 \\
		GPT-2 (tiếng Anh) \autocite{radford2019gpt2} & 50.257 & 3,970 \\
		\hline
	\end{tabular}
\end{table}

\section{Kết quả đa phương thức sau SFT}
\label{sec:exp-sft}

Bảng~\ref{tab:sft-results} tổng hợp chất lượng trên ba tập kiểm tra sau hai
pha SFT; hình~\ref{fig:demo} minh họa định tính. Ba quan sát: (i)~\emph{chênh
miền rất rõ} --- CIDEr trên UIT-ViIC (thể thao, hẹp miền: 0,325 beam3) cao gấp
$2{,}6$ lần KTVIC (đời sống, rộng miền: 0,125), cho thấy ở quy mô 101M mô hình
khái quát tốt trên miền hẹp nhưng đuối trên miền đời sống đa dạng; (ii)~beam
search giữ biên cải thiện đều so với greedy (ViIC $+0{,}012$, KTVIC $+0{,}005$
CIDEr) --- nhất quán với kết luận của đồ án cuối kỳ; (iii)~hỏi--đáp cho CIDEr
cao (1,943) nhưng chủ yếu do câu trả lời VQA rất ngắn (thường 1--5 từ) khiến
CIDEr một-tham-chiếu dễ đạt trị lớn --- BLEU-4 0,279 là chỉ số phản ánh sát
hơn. So sánh tuyệt đối: các số này thấp hơn baseline công bố của UIT-ViIC
(dùng mô hình lớn hơn và LM huấn luyện sẵn), đúng như định hướng ``đo xem một
GPU làm được đến đâu'' chứ không nhằm cạnh tranh trực tiếp.

\begin{table}[H]
	\centering
	\caption{Kết quả trên tập kiểm tra: mô tả ảnh (UIT-ViIC, KTVIC) và
	hỏi--đáp (OpenViVQA dev). B-4 = BLEU-4; C = CIDEr.}
	\label{tab:sft-results}
	\begin{tabular}{llcccc}
		\hline
		\textbf{Tập} & \textbf{Checkpoint} & \textbf{B-4 (tham lam)} & \textbf{C (tham lam)} & \textbf{B-4 (beam3)} & \textbf{C (beam3)} \\
		\hline
		UIT-ViIC & SFT & 0,171 & 0,312 & 0,186 & \textbf{0,325} \\
		KTVIC & SFT & 0,087 & 0,119 & 0,099 & 0,125 \\
		OpenViVQA & SFT & 0,279 & 1,943 & --- & --- \\
		\hline
	\end{tabular}
\end{table}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{figs/results/sft_loss.png}
	\caption{Loss SFT hai pha: pha ``projector'' (làm nóng, GPT đóng băng) rồi
	pha ``full'' (mở GPT + projector). Nguồn: \texttt{sft\_log.csv}.}
	\label{fig:sft-loss}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{figs/results/demo.png}
	\caption{Minh họa định tính: sáu ảnh kiểm tra với caption do ViVLM-nano
	sinh (giải mã tham lam, câu lệnh ``Mô tả bức ảnh.''). Hàng trên (miền
	UIT-ViIC, thể thao) và hàng dưới (miền KTVIC, đời sống). Mô hình nhận đúng
	đối tượng và hành động chính (cậu bé đá bóng, vận động viên tennis, thuyền
	trên sông, cửa hàng tạp hóa) --- bằng chứng phần thị giác thực sự được hợp
	nhất, dù mọi tham số ngôn ngữ đều tự pretrain từ đầu.}
	\label{fig:demo}
\end{figure}

\section{Đóng góp của SCST}
\label{sec:exp-scst}

Bảng~\ref{tab:scst} so sánh trước/sau 400 bước SCST trên hai tập mô tả ảnh.
Kết quả rõ hai điểm. \textbf{Một}, SCST nâng CIDEr đáng kể trên UIT-ViIC
(giải mã tham lam $0{,}312 \to 0{,}380$, tức $+0{,}068$ --- biên lớn hơn hẳn
mức $+0{,}010$ đến $+0{,}013$ quan sát ở đồ án cuối kỳ), nhưng gần như không
đổi trên KTVIC ($+0{,}007$): SCST phát huy mạnh ở miền hẹp nơi tín hiệu phần
thưởng CIDEr nhất quán, và đuối ở miền rộng nơi phần thưởng nhiễu.
\textbf{Hai}, sau SCST, giải mã tham lam ($0{,}380$) \emph{vượt} beam search
($0{,}376$) trên UIT-ViIC --- đảo chiều so với trước SCST. Đây là dấu hiệu
kinh điển của SCST: thuật toán tối ưu trực tiếp \emph{chính sách giải mã} (cụ
thể là đường tham lam dùng làm baseline), nên đường tham lam cải thiện nhiều
nhất và thu hẹp khoảng cách với beam --- củng cố kết luận từ đồ án cuối kỳ
rằng SCST tác động vào chiến lược sinh nhiều hơn là vào biểu diễn của mô hình.

\begin{table}[H]
	\centering
	\caption{CIDEr trước và sau SCST (giải mã tham lam / beam3).}
	\label{tab:scst}
	\begin{tabular}{lcc}
		\hline
		\textbf{Tập} & \textbf{SFT} & \textbf{SFT + SCST} \\
		\hline
		UIT-ViIC & 0,312 / 0,325 & \textbf{0,380} / 0,376 \\
		KTVIC & 0,119 / 0,125 & 0,126 / 0,130 \\
		\hline
	\end{tabular}
\end{table}

\section{Bàn luận}
\label{sec:exp-discussion}

Đối chiếu bốn câu hỏi nghiên cứu (mục~\ref{sec:intro-diagram}) với kết quả:

\textbf{Câu hỏi 1 (chất lượng ngôn ngữ).} Tiền huấn luyện từ đầu trên một GPU
đạt val loss 2,61 và PPL 13,91 --- đủ để mô hình sinh văn xuôi tiếng Việt
đúng ngữ pháp, mạch lạc cục bộ (bảng~\ref{tab:samples}). Đường loss tuân thủ
quy luật lũy thừa, val bám train, khẳng định pipeline pretrain vận hành đúng
ở quy mô này. Giới hạn kiến thức thế giới là có thật và lộ rõ ở hỏi--đáp, đúng
kỳ vọng cho 101M tham số / 3 tỷ token.

\textbf{Câu hỏi 2 (tokenizer).} Bộ tách từ tự huấn luyện đạt fertility ngang
PhoGPT (1,160 so với 1,166) và vượt xa GPT-2 tiếng Anh (3,970). Phát hiện
đáng giá nhất là mối liên hệ định lượng giữa fertility và BPC: baseline
NlpHUST tuy có PPL mỗi token tương đương ($\approx$13,8) nhưng dùng tokenizer
tiếng Anh nên tốn 3,4 lần token/ký tự, dẫn tới BPC cao gấp 3,4 lần --- minh
chứng rằng ở tiếng Việt, một tokenizer bản ngữ là điều kiện tiên quyết cho
hiệu quả nén, độc lập với năng lực mô hình.

\textbf{Câu hỏi 3 (đa phương thức).} Projector pixel-shuffle ghép SigLIP đóng
băng vào ViGPT-nano cho VLM biết mô tả ảnh và trả lời câu hỏi bằng tiếng Việt
(CIDEr UIT-ViIC 0,325; hình~\ref{fig:demo} xác nhận định tính). Chênh miền
mạnh (ViIC gấp 2,6 lần KTVIC) là bài học rõ nhất: ở quy mô nano, độ hẹp/đồng
nhất của miền quan trọng hơn kích thước tập dữ liệu.

\textbf{Câu hỏi 4 (SCST).} SCST nâng CIDEr UIT-ViIC $+0{,}068$ và đảo chiều
tham lam--beam, tái khẳng định trên VLM tự pretrain kết luận về bản chất
``tối ưu chính sách giải mã'' của SCST đã thấy ở đồ án cuối kỳ, với biên độ
thậm chí lớn hơn.

\textbf{So với hướng ``mượn LLM sẵn''.} Về điểm số tuyệt đối, ViVLM-nano
thấp hơn LaVy/Vintern-1B (xây trên LLM tỷ tham số huấn luyện sẵn) --- điều
tất yếu khi phần ngôn ngữ chỉ 101M và tự pretrain trong nửa ngày GPU. Giá trị
của đề tài không nằm ở việc vượt các hệ đó, mà ở chỗ định lượng \emph{trần
năng lực của một GPU} cho trọn pipeline, và ở tính minh bạch: mọi tham số
ngôn ngữ đều quan sát được từ dữ liệu thô đến checkpoint cuối, không có hộp
đen pretrained ở phần cốt lõi.
